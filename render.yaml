services:
  - type: web
    name: khusphus
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node realtime-server.js
    envVars:
      - key: NODE_ENV
        value: production
